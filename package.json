{
  "name": "datn-coolmate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug",
    "test:headed": "npx playwright test --headed",
    "allure:clean": "rm -rf allure-results",
    "allure:generate": "npx allure generate allure-results -c ./allurerc.mjs -o ./allure-report",
    "allure:open": "npx allure open allure-report",
    "allure:serve": "npx allure generate allure-results -c ./allurerc.mjs -o ./allure-report && npx allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.6.0",
    "allure-playwright": "^3.6.0"
  },
  "dependencies": {
    "dotenv": "^17.4.1"
  }
}
